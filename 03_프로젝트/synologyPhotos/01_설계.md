# synologyPhotos — 설계

---

## 프로젝트 구조

```
synologyPhotos/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                    # 로그인 페이지
│   ├── (dashboard)/
│   │   ├── layout.tsx              # 인증 가드
│   │   ├── timeline/page.tsx       # 타임라인 보기
│   │   ├── gallery/page.tsx        # 연도·장소별 보기
│   │   ├── duplicates/page.tsx     # 중복 사진 관리
│   │   ├── similar/page.tsx        # 유사 사진 관리
│   │   └── organize/page.tsx       # 폴더 정리
│   └── api/
│       ├── auth/
│       │   ├── login/route.ts
│       │   └── logout/route.ts
│       ├── photos/
│       │   ├── list/route.ts
│       │   ├── delete/route.ts
│       │   ├── move/route.ts
│       │   └── rename/route.ts
│       ├── scan/
│       │   ├── duplicates/route.ts     # MD5 해시 스캔
│       │   └── similar/route.ts        # pHash 유사도 스캔
│       └── thumbnail/[id]/route.ts     # 썸네일 프록시
├── lib/
│   ├── synology/
│   │   ├── client.ts               # DSM API fetch 래퍼
│   │   ├── auth.ts                 # 로그인·세션 관리
│   │   └── photos.ts               # 사진 CRUD
│   ├── image/
│   │   ├── hash.ts                 # MD5 중복 감지
│   │   └── phash.ts                # pHash 유사도 감지
│   ├── exif.ts                     # EXIF 메타데이터 추출
│   └── session.ts                  # iron-session 설정
├── components/
│   ├── PhotoGrid.tsx
│   ├── PhotoCard.tsx
│   ├── DuplicateGroup.tsx
│   ├── ScanProgress.tsx
│   └── FolderOrganizer.tsx
├── .env.local
└── CLAUDE.md
```

---

## 시놀리지 DSM API 핵심 엔드포인트

```
# 인증
POST /webapi/auth.cgi
  api=SYNO.API.Auth&version=7&method=login
  &account=USER&passwd=PASS&session=SynoPhoto&format=cookie

# 사진 목록
GET /webapi/entry.cgi
  ?api=SYNO.Foto.Browse.Item&version=1&method=list
  &folder_id=FOLDER_ID&offset=0&limit=100

# 폴더 목록
GET /webapi/entry.cgi
  ?api=SYNO.Foto.Browse.Folder&version=1&method=list

# 사진 삭제
POST /webapi/entry.cgi
  api=SYNO.Foto.Browse.Item&version=1&method=delete&id=[1,2,3]

# 사진 이동
POST /webapi/entry.cgi
  api=SYNO.Foto.Browse.Item&version=1&method=copy_to
  &id=[1]&target_folder_id=FOLDER_ID

# 썸네일
GET /webapi/entry.cgi
  ?api=SYNO.Foto.Thumbnail&version=2&method=get
  &id=PHOTO_ID&cache_key=KEY&type=unit&size=sm
```

---

## DB 스키마

없음 — 시놀리지 NAS가 데이터 소스. 별도 DB 불필요.

---

## 핵심 설계 결정 (ADR)

### ADR-001: 서버사이드 프록시 방식
- **결정:** 브라우저에서 시놀리지 API 직접 호출 대신, Next.js API Routes를 프록시로 사용
- **이유:** CORS 문제 회피 + 자격증명을 서버에만 보관 (세션 쿠키)
- **구현:** `lib/synology/client.ts`에서 fetch로 DSM API 호출, 세션 토큰은 iron-session 쿠키

### ADR-002: 중복 감지 2단계
- **결정:** 완전 중복은 MD5 해시, 유사 사진은 pHash (hamming distance ≤ 10)
- **이유:** MD5는 빠르고 정확, pHash는 리사이즈·필터 적용된 유사 사진도 잡음
- **구현:** 썸네일을 서버에서 받아 `sharp`로 8×8 그레이스케일 → 비트 해시 비교

### ADR-003: 스캔은 청크 단위 스트리밍 (SSE)
- **결정:** 전체 사진을 한 번에 스캔하지 않고 페이지(100장)씩 처리 후 SSE로 진행률 전송
- **이유:** 사진이 수천 장이면 단일 요청 타임아웃 위험
- **구현:** `GET /api/scan/duplicates` → `ReadableStream`으로 진행 이벤트 push

### ADR-004: 목표 폴더 구조
- **결정:** `/개인공간/YYYY/YYYY-MM`
- **이유:** EXIF 촬영일 기준으로 현재 혼재된 구조를 재정리
- **구현:** organize 페이지에서 "폴더 미분류" 파일 목록 → 이동 버튼

### ADR-005: 로컬 배포 우선
- **결정:** Vercel 배포 않고 `npm run dev`로 로컬 실행
- **이유:** 시놀리지 자격증명이 .env.local에 있어 외부 노출 위험

---

## 환경변수

```
# .env.local
SYNOLOGY_BASE_URL=https://demons815.direct.quickconnect.to:5001
SESSION_SECRET=random-32-char-secret-here
```

> 계정/비밀번호는 env에 하드코딩하지 않고, 로그인 폼에서 입력받아 세션에만 보관.
